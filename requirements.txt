fastapi
uvicorn[standard]
python-multipart
pandas
numpy
# SurPyval from git: the PyPI releases are stale and miss the proportional-
# hazards / AFT / PO / AH classes, the discrete distributions, and more. Pinned
# to a commit SHA so builds are reproducible. (Requires git at build time.)
git+https://github.com/derrynknife/SurPyval.git@v0.20.0
# RePyability is installed --no-deps in the Dockerfile, which is what makes the
# pin above authoritative — its metadata still says surpyval>=0.16,<0.17, a
# ceiling it has simply never moved rather than a real incompatibility. It only
# touches the distribution classes' fit/sf surface, which is unchanged across
# 0.16 -> 0.19; backend/tests/test_rbd_analysis.py exercises that pairing, so a
# real break would fail the suite rather than reach production. --no-deps also
# stops pip re-resolving surpyval from PyPI, where the releases are stale.
# RePyability's runtime deps (numpy/scipy via surpyval, networkx, tqdm) are
# provided explicitly here.
# Transitive deps not declared by the above / for the --no-deps repyability:
joblib
dd
tqdm
networkx>=3.0
pydantic
pymongo
mongomock
firebase-admin
stripe
httpx
# Renders the "What's new" update emails (backend/scripts/send_update.py).
markdown
# Managed Agents live in the beta namespace (beta.agents/environments/sessions);
# event-delta streaming (for our SSE) landed in 0.115.0. Pinned for reproducible
# builds against the still-evolving Managed Agents beta API.
anthropic==0.117.0
