fastapi
uvicorn[standard]
python-multipart
pandas
numpy
# SurPyval/RePyability from git: the PyPI releases are stale and miss the
# proportional-hazards classes (GammaPH, …), the discrete distributions
# (DiscreteWeibull, Geometric, NegativeBinomial) and
# repyability.rbd.non_repairable_rbd this app imports. Pinned to commit SHAs so
# builds are reproducible. (Requires git at build time — see Dockerfile.)
git+https://github.com/derrynknife/SurPyval.git@ca31a66a86a94d0a3993f8106755a5086e989071
git+https://github.com/derrynknife/RePyability.git@84b91c0975b47043bb69d3234acc243a633bd66e
# Transitive deps not declared by the above packages:
joblib
dd
tqdm
pydantic
pymongo
mongomock
firebase-admin
stripe
httpx
