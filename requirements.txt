fastapi
uvicorn[standard]
python-multipart
pandas
numpy
# SurPyval from git: the PyPI releases are stale and miss the proportional-
# hazards / AFT / PO / AH classes, the discrete distributions, and more. Pinned
# to a commit SHA so builds are reproducible. (Requires git at build time.)
git+https://github.com/derrynknife/SurPyval.git@v0.16.0
# RePyability is installed --no-deps in the Dockerfile. Its metadata now pins
# surpyval>=0.16,<0.17 (satisfied by our 0.16.0 git pin), so the old version
# conflict is gone — but we keep --no-deps so the git-pinned surpyval above stays
# authoritative and pip never re-resolves it from PyPI. RePyability's runtime deps
# (numpy/scipy via surpyval, networkx, tqdm) are provided explicitly here.
# Transitive deps not declared by the above / for the --no-deps repyability:
joblib
dd
tqdm
networkx>=3.0
pydantic
pymongo
mongomock
firebase-admin
stripe
httpx
# Managed Agents live in the beta namespace (beta.agents/environments/sessions);
# event-delta streaming (for our SSE) landed in 0.115.0. Pinned for reproducible
# builds against the still-evolving Managed Agents beta API.
anthropic==0.117.0
