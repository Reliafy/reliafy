# Guide screenshots

Generated by `node scripts/capture-guides.mjs` against a local, auth-disabled,
seeded instance of the app. Filenames are referenced from `content/guides/*.md`.
Re-run the harness after UI changes so the guides stay in sync. Until captured,
a guide shows a labelled placeholder in place of each screenshot.
